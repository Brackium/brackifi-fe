export type HeaderLink = {
  text: string;
  link: string;
};

export type Whys = {
  title: string;
  text: string;
};

export type Catalogue = {
  title: string;
  text1: string;
  text2: string;
};
